# Nginx reverse proxy for Laravel API (api-qldn.zsellers.com)
# Copy/adapt on the server. Reload: sudo nginx -t && sudo systemctl reload nginx

server {
    listen 80;
    server_name api-qldn.zsellers.com;

    # Must match PHP post_max_size / upload_max_filesize / Laravel UPLOAD_MAX_MB
    client_max_body_size 520m;

    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # Large Excel uploads / long imports
        proxy_connect_timeout 600s;
        proxy_send_timeout 7200s;
        proxy_read_timeout 7200s;
        send_timeout 7200s;
    }
}

# If using PHP-FPM + nginx directly (no artisan serve):
#
# server {
#     listen 80;
#     server_name api-qldn.zsellers.com;
#     client_max_body_size 520m;
#     root /var/www/public;
#     index index.php;
#
#     location / {
#         try_files $uri $uri/ /index.php?$query_string;
#     }
#
#     location ~ \.php$ {
#         fastcgi_pass 127.0.0.1:9000;
#         fastcgi_param SCRIPT_FILENAME $realpath_root$fastcgi_script_name;
#         include fastcgi_params;
#         fastcgi_read_timeout 7200s;
#         client_max_body_size 520m;
#     }
# }
